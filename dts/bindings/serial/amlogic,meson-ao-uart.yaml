
description: Amlogic UART

compatible: amlogic,meson-ao-uart

include: uart-controller.yaml

properties:
    interrupts:
      required: true

    reg-shift:
      type: int
      required: true
      description: quantity to shift the register offsets by

